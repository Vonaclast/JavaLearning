public class 07 {
    public static void main(String[] args) {
        int counter = 0;
        
        while (counter < 100) {
            counter++;
            System.out.println("This is sentence number #" + counter.toString());
        }
    }
}